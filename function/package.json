{
  "name": "ose-chat-function",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build:persona": "node scripts/bundle-persona.mjs",
    "build:pagemap": "node scripts/bundle-pagemap.mjs",
    "build": "npm run build:persona && npm run build:pagemap && tsc -p tsconfig.json",
    "test": "vitest run",
    "dev": "npm run build:persona && npm run build:pagemap && tsx src/server.ts"
  },
  "dependencies": {
    "@mistralai/mistralai": "^1.5.0",
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0",
    "@types/node": "^20.0.0"
  }
}
